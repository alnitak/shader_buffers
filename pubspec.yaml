name: shader_buffers
description: Using shaders to feed a shader
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.1
  
  flutter_test:
    sdk: flutter

  very_good_analysis: ^5.1.0

flutter:

  assets:
    - assets/

