name: shader_buffers_example
description: Shader buffers example
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.8
  
  flutter:
    sdk: flutter

  shader_buffers:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  
  flutter_test:
    sdk: flutter

  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

  shaders:
    - shaders/arrows.frag
    - shaders/mouse1.frag
    - shaders/mouse2.frag
    - shaders/shader1_main.frag
    - shaders/shader1_bufferA.frag
    - shaders/shader2_main.frag
    - shaders/shader2_bufferA.frag
    - shaders/test.frag
    - shaders/water.frag
    - shaders/page_curl.frag
    - shaders/noise_fade.frag
    - shaders/ripple.frag
    - shaders/test_isself_buffer_a.frag
    - shaders/test_isself_main.frag
    - shaders/filters/black.frag
    - shaders/filters/white.frag

